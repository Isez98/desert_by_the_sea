import React from 'react'
import '../../App/App.css';

const MenuList = ({children}) => {
    return (
        <div>            
             {children}
         </div>
    )
 }
 export default MenuList